// Using sonner for toasts - this is just a re-export wrapper
// eslint-disable-next-line react-refresh/only-export-components -- re-export barrel for shadcn parity
export { Toaster, toast } from "sonner";
