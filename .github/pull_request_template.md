## Summary

<!-- What does this PR change and why? Link the phase/task from task.md (e.g., "Phase 1.3 — auth"). -->

## Checklist

- [ ] Conventional commit title (`feat:`, `fix:`, `docs:`, `chore:`, ...)
- [ ] `npm run lint` passes locally
- [ ] `npm run typecheck` passes locally
- [ ] `npm run test` passes locally
- [ ] Added/updated tests for the change
- [ ] Docs / `.env.example` updated if config changed

## Test plan

<!-- How was this manually verified? Steps a reviewer can follow. -->
