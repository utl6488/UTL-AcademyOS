# Infra

Deployment, Docker, and ops manifests live here.

Populated in **Phase 12 — DevOps, Observability & Deploy**. Expected contents:

- `docker/` — production Dockerfiles + compose overrides
- `nginx/` — reverse proxy configs
- `k8s/` or `terraform/` — if we go managed
- `grafana/` — dashboards, alerts
- `runbooks/` — pointer to `docs/runbooks/`
